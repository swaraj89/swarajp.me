import * as React from 'react';
import './index.css';

const { Component } = React;

export class Contact extends Component<{}, {}> {
    render() {
        return (
            <h1>Contact</h1>
        );
    }
}